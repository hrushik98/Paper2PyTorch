# Optional: server-level Gemini API key (users can supply their own via the UI)
GOOGLE_API_KEY=

# Optional: GitHub Personal Access Token for Colab Gist creation
GITHUB_TOKEN=

# Optional: override the default 30 MB PDF size limit
MAX_UPLOAD_MB=30
